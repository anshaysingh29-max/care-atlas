'use client';
import { useEffect,useState } from 'react';
import { Copy, Link2, MessageCircle, ShieldCheck } from 'lucide-react';
import PartnerShell from '@/components/PartnerShell';
import { buildPartnerReferralUrl,getPartnerProfile } from '@/lib/firebase/partners';
export default function PartnerMarketingClient(){const[profile,setProfile]=useState(null);const[url,setUrl]=useState('');const[copied,setCopied]=useState(false);useEffect(()=>{getPartnerProfile().then(p=>{setProfile(p);setUrl(buildPartnerReferralUrl(p?.referralCode));});},[]);async function copy(){if(!url)return;await navigator.clipboard.writeText(url);setCopied(true);setTimeout(()=>setCopied(false),1500);}const msg=`Explore international treatment options with CareAtlas: ${url}`;return <PartnerShell title="Marketing tools" subtitle="Share a tracked CareAtlas introduction without making medical claims or promising outcomes."><div className="phase7a-marketing-grid"><section className="portal-card"><Link2 size={22}/><span className="eyebrow">TRACKED LINK</span><h2>{profile?.referralCode||'Loading…'}</h2><div className="phase7a-share-line"><code>{url||'Generating link…'}</code><button type="button" onClick={copy} disabled={profile?.status!=='approved'}><Copy size={15}/>{copied?'Copied':'Copy'}</button></div>{profile?.status==='approved'&&url?<a className="button button-sm phase7a-whatsapp" target="_blank" rel="noreferrer" href={`https://wa.me/?text=${encodeURIComponent(msg)}`}><MessageCircle size={16}/> Share on WhatsApp</a>:<p className="phase7a-muted">Your link activates after CareAtlas approves the partner application.</p>}</section><section className="portal-card phase7a-marketing-rules"><ShieldCheck size={22}/><span className="eyebrow">SAFE PROMOTION</span><h2>What you can say.</h2><p>Introduce CareAtlas as a medical-travel coordination marketplace. Do not promise cures, guaranteed outcomes, doctor superiority or undisclosed discounts.</p><p>Partners may receive compensation for successful introductions. Compensation must not change clinical recommendations or hospital ranking.</p></section></div></PartnerShell>}
