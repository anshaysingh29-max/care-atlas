import HospitalCompare from '@/components/HospitalCompare';

export const metadata = {
  title: 'Compare Hospitals — CareAtlas',
  description: 'Compare CareAtlas hospitals across destinations, services and indicative treatment information.'
};

export default function ComparePage() {
  return <HospitalCompare />;
}
