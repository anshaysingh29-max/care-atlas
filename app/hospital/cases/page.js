import HospitalCasesClient from '@/components/HospitalCasesClient';
export const metadata = { title: 'Patient Cases — CareAtlas Hospital Portal' };
export default function HospitalCasesPage() { return <HospitalCasesClient/>; }
